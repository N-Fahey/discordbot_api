# daCordbot_api

```
docker run -d \
  -e "DB_USER=user" \
  -e "DB_PASSWORD=password" \
  -e "DB_HOST=host" \
  -e "DB_PORT=port" \
  -e "DB_NAME=db_name" \
  -p 127.0.0.1:8000:8000 \
  daCordbot-api:latest
```
